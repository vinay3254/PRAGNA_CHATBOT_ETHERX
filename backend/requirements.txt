flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.8.0
python-dotenv==1.0.0
requests==2.31.0
gTTS==2.5.0
gunicorn==21.2.0
groq==0.11.0
PyJWT==2.12.1
python-dateutil==2.8.2
bcrypt==4.1.1

# RAG and NLP dependencies (optional for enhanced retrieval)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4

# Web scraping (optional for dynamic content updates)
beautifulsoup4>=4.12.0

# Upload/document analysis
pypdf>=4.3.1
python-docx>=1.1.2
openpyxl>=3.1.5
python-pptx>=1.0.2
reportlab>=4.2.0

# ─────────────────────────────────────────────────────────────
# DeepSeek-R1 Local Inference (HuggingFace Transformers)
# ─────────────────────────────────────────────────────────────
# Install CPU-only build by default (smaller, works on any machine):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Switch to CUDA (NVIDIA GPU) — zero code changes needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   (or cu118, cu124 — match your installed CUDA version)
#
transformers>=4.40.0
torch>=2.1.0
accelerate>=0.26.0
sentencepiece>=0.1.99
safetensors>=0.4.0
Pillow>=10.0.0
